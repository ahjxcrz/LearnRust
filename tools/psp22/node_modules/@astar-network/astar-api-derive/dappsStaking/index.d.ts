import { ApiInterfaceRx } from '@polkadot/api/types';
import { Observable } from 'rxjs';
import { AccountId } from '@polkadot/types/interfaces';
import { ContractAddress } from '../types';
export declare const getAddressEnum: (address: string) => {
    Evm: string;
};
/**
 * @deprecated stakers does not return results
 */
export declare function stakers(instanceId: string, api: ApiInterfaceRx): (contractAddress: ContractAddress) => Observable<AccountId[]>;
